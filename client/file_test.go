package client

import (
	"testing"
)

func TestFile_Share(t *testing.T) {
	/*network := nw.Network{"http://0.0.0.0:6000"}
	f := FilePTP{"path", "hash", "owner", []string{"hali", "gali"}, []string{}}
	err := f.Share([]string{"gali", "alma"}, "./data/public/for/", &network, )
	if err != nil {
		t.Fatal(err.Error())
	}
	fmt.Println(f.SharedWith)*/
}
